<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/><meta name="viewport" content="width=device-width,initial-scale=1.0"/>
<title>Usuarios · Admin · Centro Médico Azucena</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
:root{--blue:#0066FF;--blue2:#0047B3;--card:#0a1628;--border:rgba(255,255,255,.07);--muted:#7B8CB0;--grn:#16a34a;--red:#dc2626;--r:12px}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
body{font-family:'Outfit',sans-serif;background:#080f1e;color:#fff;display:flex;min-height:100vh;overflow:hidden}
aside{width:240px;background:#040B1C;border-right:1px solid var(--border);display:flex;flex-direction:column;flex-shrink:0;height:100vh;overflow-y:auto;position:fixed;top:0;left:0;z-index:100}
.logo{display:flex;align-items:center;gap:.55rem;padding:1.4rem 1.2rem;border-bottom:1px solid var(--border);text-decoration:none}
.lm{width:30px;height:30px;border-radius:7px;background:var(--blue);display:flex;align-items:center;justify-content:center;position:relative;flex-shrink:0}
.lm::before,.lm::after{content:'';position:absolute;background:#fff;border-radius:2px}
.lm::before{width:3px;height:17px}.lm::after{width:17px;height:3px}
.lt{font-family:'Playfair Display',serif;font-size:1.1rem;font-weight:700;color:#fff}.lt em{font-style:normal;color:#60b4ff}
.s-sec{font-size:.6rem;letter-spacing:2px;text-transform:uppercase;color:rgba(255,255,255,.25);padding:.9rem 1.2rem .35rem}
.si{display:flex;align-items:center;gap:.7rem;padding:.58rem 1.2rem;color:rgba(255,255,255,.55);text-decoration:none;font-size:.82rem;font-weight:500;transition:all .2s;cursor:pointer;border:none;background:none;width:100%;text-align:left;position:relative}
.si:hover{background:rgba(255,255,255,.04);color:#fff}
.si.active{background:rgba(0,102,255,.18);color:#60b4ff}
.si.active::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--blue);border-radius:0 3px 3px 0}
.sico{width:18px;text-align:center;font-size:.9rem;flex-shrink:0}
.main{margin-left:240px;flex:1;display:flex;flex-direction:column;min-height:100vh;overflow-y:auto}
.topbar{background:#040B1C;border-bottom:1px solid var(--border);padding:.9rem 2rem;display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:50}
.tb-l h1{font-family:'Playfair Display',serif;font-size:1.25rem;color:#fff}
.tb-l span{font-size:.72rem;color:var(--muted);display:block;margin-top:.1rem}
.tb-r{display:flex;align-items:center;gap:.7rem}
.btn-out{background:rgba(220,38,38,.15);border:1px solid rgba(220,38,38,.3);color:#fca5a5;padding:.4rem .9rem;border-radius:20px;font-family:'Outfit',sans-serif;font-size:.75rem;font-weight:600;cursor:pointer}
.btn-add{background:var(--blue);color:#fff;border:none;padding:.45rem 1.1rem;border-radius:20px;font-family:'Outfit',sans-serif;font-size:.75rem;font-weight:600;cursor:pointer;transition:all .2s}
.btn-add:hover{background:var(--blue2)}
.content{padding:1.5rem 2rem;flex:1}
/* KPI */
.kpi-row{display:grid;grid-template-columns:repeat(4,1fr);gap:.9rem;margin-bottom:1.2rem}
.kpi{background:var(--card);border:1px solid var(--border);border-radius:var(--r);padding:1.1rem;position:relative;overflow:hidden;transition:transform .2s}
.kpi:hover{transform:translateY(-2px)}
.kpi-bar{position:absolute;top:0;left:0;right:0;height:3px}
.kpi-v{font-family:'Playfair Display',serif;font-size:1.7rem;font-weight:700;color:#fff}
.kpi-l{font-size:.7rem;color:var(--muted);margin-top:.2rem}
/* GRID 2 COL */
.g2{display:grid;grid-template-columns:1fr 1.5fr;gap:1.4rem}
/* CARD */
.card{background:var(--card);border:1px solid var(--border);border-radius:var(--r);padding:1.4rem;margin-bottom:1.2rem}
.chd{display:flex;align-items:center;justify-content:space-between;margin-bottom:1.2rem;flex-wrap:wrap;gap:.75rem}
.ctit{font-family:'Playfair Display',serif;font-size:1.05rem;font-weight:700;color:#fff}
.csub{font-size:.72rem;color:var(--muted);margin-top:.1rem}
/* FORM */
.fg{display:flex;flex-direction:column;gap:.38rem;margin-bottom:.95rem}
.fg label{font-size:.72rem;font-weight:600;color:rgba(255,255,255,.5);text-transform:uppercase;letter-spacing:1px}
.fg input,.fg select{padding:.72rem .9rem;border:1.5px solid rgba(255,255,255,.1);border-radius:9px;background:rgba(255,255,255,.05);color:#fff;font-family:'Outfit',sans-serif;font-size:.87rem;outline:none;transition:all .2s}
.fg input:focus,.fg select:focus{border-color:var(--blue);background:rgba(0,102,255,.08)}
.fg input::placeholder{color:rgba(255,255,255,.25)}
.fg select option{background:#0a1628;color:#fff}
.frow{display:grid;grid-template-columns:1fr 1fr;gap:1rem}
/* ROL CARDS */
.rol-grid{display:grid;grid-template-columns:1fr 1fr;gap:.75rem;margin-bottom:1rem}
.rol-card{background:rgba(255,255,255,.04);border:1.5px solid rgba(255,255,255,.09);border-radius:10px;padding:.9rem;cursor:pointer;transition:all .2s;text-align:center}
.rol-card:hover{border-color:rgba(0,102,255,.4)}
.rol-card.sel{border-color:var(--blue);background:rgba(0,102,255,.14)}
.rol-card.sel::after{content:'✓';display:block;color:#60b4ff;font-size:.75rem;font-weight:700;margin-top:.2rem}
.rol-ico{font-size:1.5rem;margin-bottom:.3rem}
.rol-lbl{font-size:.8rem;font-weight:700;color:rgba(255,255,255,.8)}
.rol-sub{font-size:.68rem;color:rgba(255,255,255,.4);margin-top:.2rem}
/* PWD */
.pwd-w{position:relative}
.pwd-w input{width:100%;padding-right:2.8rem}
.pwd-eye{position:absolute;right:.85rem;top:50%;transform:translateY(-50%);background:none;border:none;cursor:pointer;color:rgba(255,255,255,.35);font-size:1rem}
.pwd-eye:hover{color:rgba(255,255,255,.7)}
.pwd-rules{font-size:.7rem;color:rgba(255,255,255,.3);margin-top:.35rem;line-height:1.6}
/* INFO BOX */
.info-box{background:rgba(0,102,255,.08);border:1px solid rgba(0,102,255,.2);border-radius:9px;padding:.85rem 1rem;font-size:.8rem;color:#a0c4ff;margin-bottom:1rem;line-height:1.6}
.warn-box{background:rgba(202,138,4,.08);border:1px solid rgba(202,138,4,.2);border-radius:9px;padding:.85rem 1rem;font-size:.8rem;color:#fde047;margin-bottom:1rem;line-height:1.6}
/* BTN SAVE */
.btn-save{width:100%;background:var(--blue);color:#fff;border:none;padding:.92rem;border-radius:50px;font-family:'Outfit',sans-serif;font-size:.93rem;font-weight:700;cursor:pointer;box-shadow:0 6px 24px rgba(0,102,255,.35);transition:all .25s;display:flex;align-items:center;justify-content:center;gap:.5rem;margin-top:.5rem}
.btn-save:hover{background:var(--blue2);transform:translateY(-2px)}
.btn-save:disabled{opacity:.6;cursor:not-allowed;transform:none}
/* LISTA USUARIOS */
.u-list{display:flex;flex-direction:column;gap:.7rem}
.u-item{background:rgba(255,255,255,.04);border:1px solid var(--border);border-radius:10px;padding:1rem 1.2rem;display:flex;align-items:center;gap:.9rem;transition:border-color .2s}
.u-item:hover{border-color:rgba(0,102,255,.25)}
.u-av{width:40px;height:40px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:1.1rem;flex-shrink:0}
.u-nm{font-weight:700;font-size:.88rem;color:#fff}
.u-email{font-size:.73rem;color:var(--muted);margin-top:.1rem}
.u-rol-tag{padding:.2rem .65rem;border-radius:20px;font-size:.68rem;font-weight:700;flex-shrink:0}
.r-admin{background:rgba(220,38,38,.18);color:#fca5a5}
.r-medico{background:rgba(0,102,255,.18);color:#93c5fd}
.r-paciente{background:rgba(22,163,74,.18);color:#86efac}
.r-estudiante{background:rgba(124,58,237,.18);color:#c4b5fd}
.u-status{width:8px;height:8px;border-radius:50%;flex-shrink:0}
.u-actions{display:flex;gap:.4rem;margin-left:auto;flex-shrink:0}
.btn-sm{padding:.3rem .7rem;border-radius:7px;border:none;font-family:'Outfit',sans-serif;font-size:.72rem;font-weight:600;cursor:pointer;transition:all .2s}
.bb{background:rgba(0,102,255,.2);color:#60b4ff}.bb:hover{background:rgba(0,102,255,.4)}
.by{background:rgba(202,138,4,.2);color:#fde047}.by:hover{background:rgba(202,138,4,.4)}
.br{background:rgba(220,38,38,.15);color:#fca5a5}.br:hover{background:rgba(220,38,38,.3)}
.bg2{background:rgba(22,163,74,.2);color:#86efac}.bg2:hover{background:rgba(22,163,74,.4)}
/* MODAL RESET PWD */
.modal-bg{display:none;position:fixed;inset:0;z-index:500;background:rgba(0,0,0,.72);backdrop-filter:blur(6px);align-items:center;justify-content:center}
.modal-bg.open{display:flex}
.modal{background:#0a1628;border:1px solid rgba(255,255,255,.1);border-radius:18px;width:min(480px,95vw);max-height:90vh;overflow-y:auto;box-shadow:0 30px 80px rgba(0,0,0,.5);animation:mIn .3s ease}
@keyframes mIn{from{opacity:0;transform:scale(.95)}to{opacity:1;transform:scale(1)}}
.mhd{padding:1.4rem 1.7rem 1rem;border-bottom:1px solid var(--border);display:flex;align-items:flex-start;justify-content:space-between}
.mtit{font-family:'Playfair Display',serif;font-size:1.2rem;font-weight:700;color:#fff}
.msub{font-size:.72rem;color:var(--muted);margin-top:.15rem}
.mx{background:none;border:none;cursor:pointer;font-size:1.3rem;color:var(--muted)}
.mx:hover{color:#fff}
.mbdy{padding:1.3rem 1.7rem 1.7rem}
/* ALERT */
.al{padding:.75rem 1rem;border-radius:9px;font-size:.8rem;margin-bottom:1rem;display:none}
.al.show{display:block}
.al-ok{background:rgba(22,163,74,.15);border:1px solid rgba(22,163,74,.3);color:#86efac}
.al-err{background:rgba(220,38,38,.15);border:1px solid rgba(220,38,38,.3);color:#fca5a5}
.empty{text-align:center;color:var(--muted);padding:2rem;font-size:.85rem}
/* FILTRO */
.f-tabs{display:flex;gap:.5rem;margin-bottom:1.2rem}
.ft{padding:.42rem 1rem;border-radius:50px;border:1.5px solid rgba(255,255,255,.1);color:rgba(255,255,255,.5);font-size:.78rem;font-weight:600;cursor:pointer;background:none;font-family:'Outfit',sans-serif;transition:all .2s}
.ft.active{background:var(--blue);border-color:var(--blue);color:#fff}
@media(max-width:1000px){.g2{grid-template-columns:1fr}aside{display:none}.main{margin-left:0}.kpi-row{grid-template-columns:1fr 1fr}.frow{grid-template-columns:1fr}}
</style>
</head>
<body>
<aside>
  <a class="logo" href="../index.html" target="_blank"><div class="lm"></div><span class="lt">Azucena <em>Medical</em></span></a>
  <div class="s-sec">Principal</div>
  <a href="dashboard.html" class="si"><span class="sico">📊</span> Dashboard</a>
  <a href="agenda.html"    class="si"><span class="sico">📅</span> Agenda</a>
  <a href="citas.html"     class="si"><span class="sico">🩺</span> Citas</a>
  <div class="s-sec">Gestión</div>
  <a href="medicos.html"    class="si"><span class="sico">👨‍⚕️</span> Médicos</a>
  <a href="servicios.html"  class="si"><span class="sico">🏥</span> Servicios</a>
  <a href="pacientes.html"  class="si"><span class="sico">👥</span> Pacientes</a>
  <a href="historias.html"  class="si"><span class="sico">📋</span> Historias Clínicas</a>
  <a href="reporte.html"    class="si"><span class="sico">💰</span> Reporte Diario</a>
  <a href="estudiantes.html"class="si"><span class="sico">🎓</span> Estudiantes</a>
  <a href="certificados.html"class="si"><span class="sico">📜</span> Certificados MSP</a>
  <div class="s-sec">Personalización</div>
  <a href="publicidad.html" class="si"><span class="sico">📢</span> Publicidad</a>
  <a href="usuarios.html"   class="si active"><span class="sico">👤</span> Usuarios</a>
  <div class="s-sec">Cuenta</div>
  <a href="../login.html?action=cambio" class="si"><span class="sico">🔑</span> Cambiar contraseña</a>
  <button class="si" onclick="doLogout()"><span class="sico">🚪</span> Cerrar sesión</button>
</aside>

<div class="main">
  <div class="topbar">
    <div class="tb-l"><h1>👤 Gestión de Usuarios</h1><span>Administra accesos al sistema — admin, médicos, pacientes, estudiantes</span></div>
    <div class="tb-r"><button class="btn-out" onclick="doLogout()">🚪 Salir</button></div>
  </div>

  <div class="content">
    <div class="al al-ok" id="al-ok"></div>
    <div class="al al-err" id="al-err"></div>

    <!-- KPIs -->
    <div class="kpi-row">
      <div class="kpi"><div class="kpi-bar" style="background:linear-gradient(90deg,#0066FF,#00C2FF)"></div><div class="kpi-v" id="k-total">–</div><div class="kpi-l">Total usuarios</div></div>
      <div class="kpi"><div class="kpi-bar" style="background:linear-gradient(90deg,#dc2626,#ef4444)"></div><div class="kpi-v" id="k-admin">–</div><div class="kpi-l">👑 Admins</div></div>
      <div class="kpi"><div class="kpi-bar" style="background:linear-gradient(90deg,#0066FF,#0047B3)"></div><div class="kpi-v" id="k-medico">–</div><div class="kpi-l">👨‍⚕️ Médicos</div></div>
      <div class="kpi"><div class="kpi-bar" style="background:linear-gradient(90deg,#16a34a,#22c55e)"></div><div class="kpi-v" id="k-paciente">–</div><div class="kpi-l">🧑 Pacientes</div></div>
    </div>

    <div class="g2">
      <!-- FORM CREAR USUARIO -->
      <div>
        <div class="card">
          <div class="chd"><div><div class="ctit" id="form-tit">➕ Crear nuevo usuario</div><div class="csub">Se crea en Firebase Auth y Firestore</div></div>
            <button class="btn-sm bb" onclick="resetForm()" style="padding:.4rem .85rem">+ Nuevo</button>
          </div>
          <input type="hidden" id="edit-uid"/>

          <div class="info-box">ℹ️ El usuario (correo) no puede cambiarse después de crearlo. Médicos y estudiantes deberán cambiar su contraseña en el primer ingreso.</div>

          <!-- ROL -->
          <div style="font-size:.72rem;font-weight:600;color:rgba(255,255,255,.5);text-transform:uppercase;letter-spacing:1px;margin-bottom:.7rem">Rol del usuario *</div>
          <div class="rol-grid">
            <div class="rol-card" id="rc-admin" onclick="selRol('admin',this)"><div class="rol-ico">👑</div><div class="rol-lbl">Administrador</div><div class="rol-sub">Acceso total al sistema</div></div>
            <div class="rol-card sel" id="rc-medico" onclick="selRol('medico',this)"><div class="rol-ico">👨‍⚕️</div><div class="rol-lbl">Médico</div><div class="rol-sub">Agenda y su información</div></div>
            <div class="rol-card" id="rc-paciente" onclick="selRol('paciente',this)"><div class="rol-ico">🧑</div><div class="rol-lbl">Paciente</div><div class="rol-sub">Portal de citas e historial</div></div>
            <div class="rol-card" id="rc-estudiante" onclick="selRol('estudiante',this)"><div class="rol-ico">🎓</div><div class="rol-lbl">Estudiante</div><div class="rol-sub">Registro de prácticas</div></div>
          </div>

          <div class="frow">
            <div class="fg"><label>Nombre *</label><input id="u-nom" placeholder="Juan"/></div>
            <div class="fg"><label>Apellido *</label><input id="u-ape" placeholder="Pérez"/></div>
          </div>
          <div class="fg"><label>Correo electrónico (usuario) *</label>
            <input type="email" id="u-email" placeholder="medico@azucena.com"/>
          </div>
          <div class="fg"><label>Cédula</label>
            <input id="u-ced" placeholder="1712345678"/>
          </div>
          <div class="fg"><label>Teléfono</label>
            <input id="u-tel" placeholder="+593 99 xxx xxxx"/>
          </div>

          <!-- PASSWORD solo en modo crear -->
          <div id="pwd-section">
            <div class="fg"><label>Contraseña inicial *</label>
              <div class="pwd-w">
                <input type="password" id="u-pwd" placeholder="Mínimo 6 caracteres"/>
                <button class="pwd-eye" type="button" onclick="togglePwd('u-pwd',this)">👁</button>
              </div>
              <div class="pwd-rules">Mínimo 6 caracteres. El usuario deberá cambiarla en su primer ingreso.</div>
            </div>
          </div>

          <div id="primer-login-row" style="display:flex;align-items:center;justify-content:space-between;background:rgba(255,255,255,.04);border:1px solid var(--border);border-radius:9px;padding:.8rem 1rem;margin-bottom:1rem">
            <div><div style="font-size:.85rem;color:rgba(255,255,255,.8);font-weight:500">Forzar cambio de contraseña</div>
              <div style="font-size:.72rem;color:var(--muted);margin-top:.1rem">El usuario deberá cambiarla al primer ingreso</div></div>
            <label style="position:relative;width:44px;height:24px;flex-shrink:0">
              <input type="checkbox" id="u-primer" checked style="opacity:0;width:0;height:0"/>
              <span id="slider-primer" style="position:absolute;inset:0;background:var(--blue);border-radius:24px;cursor:pointer;transition:.3s">
                <span style="position:absolute;height:18px;width:18px;left:3px;bottom:3px;background:#fff;border-radius:50%;transition:.3s;transform:translateX(20px)"></span>
              </span>
            </label>
          </div>

          <button class="btn-save" id="btn-save" onclick="guardarUsuario()">💾 Crear usuario</button>
        </div>
      </div>

      <!-- LISTA USUARIOS -->
      <div class="card">
        <div class="chd">
          <div><div class="ctit">👥 Usuarios del sistema</div><div class="csub" id="u-count">Cargando...</div></div>
          <input type="text" id="f-bus" placeholder="🔍 Buscar..." oninput="renderUsuarios()" style="padding:.45rem .85rem;border:1.5px solid rgba(255,255,255,.1);border-radius:9px;background:rgba(255,255,255,.05);color:#fff;font-family:Outfit,sans-serif;font-size:.82rem;outline:none;min-width:180px"/>
        </div>
        <!-- FILTRO POR ROL -->
        <div class="f-tabs">
          <button class="ft active" onclick="filtrarRol('',this)">Todos</button>
          <button class="ft" onclick="filtrarRol('admin',this)">👑 Admin</button>
          <button class="ft" onclick="filtrarRol('medico',this)">👨‍⚕️ Médicos</button>
          <button class="ft" onclick="filtrarRol('paciente',this)">🧑 Pacientes</button>
          <button class="ft" onclick="filtrarRol('estudiante',this)">🎓 Estudiantes</button>
        </div>
        <div class="u-list" id="u-list"><div class="empty">Cargando usuarios...</div></div>
      </div>
    </div>
  </div>
</div>

<!-- MODAL RESET CONTRASEÑA -->
<div class="modal-bg" id="mbg-pwd">
  <div class="modal">
    <div class="mhd">
      <div><div class="mtit">🔑 Resetear contraseña</div><div class="msub" id="reset-sub">–</div></div>
      <button class="mx" onclick="cerrarReset()">✕</button>
    </div>
    <div class="mbdy">
      <input type="hidden" id="reset-uid"/>
      <div class="warn-box">⚠️ Esto cambiará la contraseña del usuario inmediatamente. El usuario deberá volver a ingresar.</div>
      <div class="fg"><label>Nueva contraseña *</label>
        <div class="pwd-w">
          <input type="password" id="r-pwd" placeholder="Mínimo 6 caracteres"/>
          <button class="pwd-eye" type="button" onclick="togglePwd('r-pwd',this)">👁</button>
        </div>
      </div>
      <div class="fg"><label>Confirmar contraseña *</label>
        <div class="pwd-w">
          <input type="password" id="r-pwd2" placeholder="Repite la contraseña"/>
          <button class="pwd-eye" type="button" onclick="togglePwd('r-pwd2',this)">👁</button>
        </div>
      </div>
      <button class="btn-save" id="btn-reset" onclick="ejecutarReset()">🔑 Cambiar contraseña</button>
    </div>
  </div>
</div>

<script type="module">
import{initializeApp}from"https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
import{getFirestore,collection,getDocs,doc,getDoc,setDoc,updateDoc,deleteDoc,serverTimestamp}
  from"https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";
import{getAuth,onAuthStateChanged,signOut,
        createUserWithEmailAndPassword,updatePassword,
        EmailAuthProvider,reauthenticateWithCredential}
  from"https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";

const FC={apiKey:"AIzaSyAwspV-1KcllVyRAbajVPLc0lwsWMOLIco",authDomain:"jadi-salud.firebaseapp.com",
  projectId:"jadi-salud",storageBucket:"jadi-salud.firebasestorage.app",
  messagingSenderId:"679691723583",appId:"1:679691723583:web:4235a2493d09a9196ea98a"};
const app=initializeApp(FC);const db=getFirestore(app);const auth=getAuth(app);

let currentAdmin=null;
onAuthStateChanged(auth,async u=>{
  if(!u){window.location.href='../login.html';return;}
  try{
    const s=await getDoc(doc(db,'usuarios',u.uid));
    if(!s.exists()||s.data().rol!=='admin'){
      // Solo admin puede gestionar usuarios
      window.location.href='../login.html';return;
    }
    currentAdmin=u;
  }catch(e){}
  loadUsuarios();
});
window.doLogout=async()=>{await signOut(auth);window.location.href='../login.html';};

function showOk(m){const e=document.getElementById('al-ok');e.textContent=m;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),5000);}
function showErr(m){const e=document.getElementById('al-err');e.textContent=m;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),7000);}

let allUsers=[];let rolFiltro='';let rolSel='medico';

// ROL SELECTOR
window.selRol=(rol,el)=>{
  rolSel=rol;
  document.querySelectorAll('.rol-card').forEach(c=>c.classList.remove('sel'));
  el.classList.add('sel');
};

window.togglePwd=(id,btn)=>{
  const i=document.getElementById(id);
  i.type=i.type==='password'?'text':'password';
  btn.textContent=i.type==='password'?'👁':'🙈';
};

// CARGAR USUARIOS
async function loadUsuarios(){
  try{
    const snap=await getDocs(collection(db,'usuarios'));
    allUsers=snap.docs.map(d=>({uid:d.id,...d.data()}));
  }catch(e){allUsers=[];}
  calcKpis();renderUsuarios();
}

function calcKpis(){
  document.getElementById('k-total').textContent=allUsers.length;
  document.getElementById('k-admin').textContent=allUsers.filter(u=>u.rol==='admin').length;
  document.getElementById('k-medico').textContent=allUsers.filter(u=>u.rol==='medico').length;
  document.getElementById('k-paciente').textContent=allUsers.filter(u=>u.rol==='paciente').length;
}

window.filtrarRol=(r,btn)=>{
  rolFiltro=r;
  document.querySelectorAll('.ft').forEach(t=>t.classList.remove('active'));
  btn.classList.add('active');
  renderUsuarios();
};

window.renderUsuarios=()=>{
  const bus=(document.getElementById('f-bus').value||'').toLowerCase();
  let f=allUsers.filter(u=>{
    if(rolFiltro&&u.rol!==rolFiltro)return false;
    if(bus&&!`${u.nombre||''} ${u.apellido||''} ${u.email||''} ${u.cedula||''}`.toLowerCase().includes(bus))return false;
    return true;
  }).sort((a,b)=>`${a.nombre} ${a.apellido}`.localeCompare(`${b.nombre} ${b.apellido}`));

  document.getElementById('u-count').textContent=`${f.length} de ${allUsers.length} usuarios`;

  const rolTag={
    admin:'<span class="u-rol-tag r-admin">👑 Admin</span>',
    medico:'<span class="u-rol-tag r-medico">👨‍⚕️ Médico</span>',
    paciente:'<span class="u-rol-tag r-paciente">🧑 Paciente</span>',
    estudiante:'<span class="u-rol-tag r-estudiante">🎓 Estudiante</span>'
  };
  const rolAv={admin:'👑',medico:'👨‍⚕️',paciente:'🧑',estudiante:'🎓'};
  const rolBg={admin:'rgba(220,38,38,.25)',medico:'rgba(0,102,255,.25)',paciente:'rgba(22,163,74,.25)',estudiante:'rgba(124,58,237,.25)'};

  const list=document.getElementById('u-list');
  if(f.length===0){list.innerHTML='<div class="empty">No se encontraron usuarios.</div>';return;}

  list.innerHTML=f.map(u=>`
    <div class="u-item">
      <div class="u-av" style="background:${rolBg[u.rol]||'rgba(0,102,255,.25)'}">${rolAv[u.rol]||'👤'}</div>
      <div style="flex:1;min-width:0">
        <div class="u-nm">${u.nombre||'–'} ${u.apellido||''}</div>
        <div class="u-email">${u.email||'–'} ${u.cedula?'· '+u.cedula:''}</div>
        ${u.primerLogin?'<div style="font-size:.68rem;color:#fde047;margin-top:.15rem">⚠️ Debe cambiar contraseña</div>':''}
      </div>
      ${rolTag[u.rol]||''}
      <div class="u-status" style="background:${u.activo!==false?'#86efac':'#fca5a5'}"></div>
      <div class="u-actions">
        <button class="btn-sm bb" onclick="editarUsuario('${u.uid}')">✏️ Editar</button>
        <button class="btn-sm by" onclick="abrirReset('${u.uid}','${u.email||''}','${u.nombre||''} ${u.apellido||''}')">🔑 Clave</button>
        <button class="btn-sm ${u.activo!==false?'br':'bg2'}" onclick="toggleActivo('${u.uid}',${u.activo!==false})">${u.activo!==false?'⏸':'▶'}</button>
      </div>
    </div>`).join('');
};

// CREAR USUARIO
window.guardarUsuario=async()=>{
  const editUid=document.getElementById('edit-uid').value;
  const nom=document.getElementById('u-nom').value.trim();
  const ape=document.getElementById('u-ape').value.trim();
  const email=document.getElementById('u-email').value.trim();
  const ced=document.getElementById('u-ced').value.trim();
  const tel=document.getElementById('u-tel').value.trim();
  const pwd=document.getElementById('u-pwd').value;
  const primer=document.getElementById('u-primer').checked;

  if(!nom||!email){showErr('Nombre y correo son obligatorios.');return;}
  if(!editUid&&pwd.length<6){showErr('La contraseña debe tener al menos 6 caracteres.');return;}

  const btn=document.getElementById('btn-save');btn.disabled=true;btn.innerHTML='⏳ Guardando...';

  try{
    if(editUid){
      // EDITAR — solo datos en Firestore (no email, no password aquí)
      await updateDoc(doc(db,'usuarios',editUid),{
        nombre:nom,apellido:ape,cedula:ced,telefono:tel,
        rol:rolSel,updatedAt:serverTimestamp()
      });
      showOk('✅ Usuario actualizado correctamente.');
    }else{
      // CREAR nuevo usuario en Firebase Auth
      const cred=await createUserWithEmailAndPassword(auth,email,pwd);
      await setDoc(doc(db,'usuarios',cred.user.uid),{
        uid:cred.user.uid,nombre:nom,apellido:ape,
        email,cedula:ced,telefono:tel,
        rol:rolSel,activo:true,
        primerLogin:primer,
        creadoEn:serverTimestamp()
      });
      showOk(`✅ Usuario creado con rol ${rolSel}. ${primer?'Deberá cambiar su contraseña al ingresar.':''}`);
    }
    resetForm();loadUsuarios();
  }catch(e){
    const msgs={
      'auth/email-already-in-use':'Ya existe un usuario con ese correo.',
      'auth/invalid-email':'El formato del correo no es válido.',
      'auth/weak-password':'La contraseña es muy débil.'
    };
    showErr(msgs[e.code]||'Error: '+e.message);
  }
  btn.disabled=false;btn.innerHTML='💾 Crear usuario';
};

window.editarUsuario=(uid)=>{
  const u=allUsers.find(x=>x.uid===uid);if(!u)return;
  document.getElementById('edit-uid').value=uid;
  document.getElementById('form-tit').textContent='✏️ Editar usuario';
  document.getElementById('u-nom').value=u.nombre||'';
  document.getElementById('u-ape').value=u.apellido||'';
  document.getElementById('u-email').value=u.email||'';
  document.getElementById('u-email').disabled=true;// No cambiar correo
  document.getElementById('u-ced').value=u.cedula||'';
  document.getElementById('u-tel').value=u.telefono||'';
  document.getElementById('pwd-section').style.display='none';
  document.getElementById('primer-login-row').style.display='none';
  document.getElementById('btn-save').innerHTML='💾 Guardar cambios';
  rolSel=u.rol||'paciente';
  document.querySelectorAll('.rol-card').forEach(c=>c.classList.remove('sel'));
  document.getElementById('rc-'+rolSel)?.classList.add('sel');
  window.scrollTo({top:0,behavior:'smooth'});
};

window.resetForm=()=>{
  document.getElementById('edit-uid').value='';
  document.getElementById('form-tit').textContent='➕ Crear nuevo usuario';
  ['u-nom','u-ape','u-email','u-ced','u-tel','u-pwd'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('u-email').disabled=false;
  document.getElementById('pwd-section').style.display='';
  document.getElementById('primer-login-row').style.display='flex';
  document.getElementById('u-primer').checked=true;
  document.getElementById('btn-save').innerHTML='💾 Crear usuario';
  rolSel='medico';
  document.querySelectorAll('.rol-card').forEach(c=>c.classList.remove('sel'));
  document.getElementById('rc-medico').classList.add('sel');
};

window.toggleActivo=async(uid,activo)=>{
  try{await updateDoc(doc(db,'usuarios',uid),{activo:!activo,updatedAt:serverTimestamp()});
    showOk(`Usuario ${!activo?'activado':'desactivado'}.`);loadUsuarios();}
  catch(e){showErr('Error: '+e.message);}
};

// RESET CONTRASEÑA
window.abrirReset=(uid,email,nombre)=>{
  document.getElementById('reset-uid').value=uid;
  document.getElementById('reset-sub').textContent=`${nombre} · ${email}`;
  document.getElementById('r-pwd').value='';
  document.getElementById('r-pwd2').value='';
  document.getElementById('mbg-pwd').classList.add('open');
};
window.cerrarReset=()=>document.getElementById('mbg-pwd').classList.remove('open');
document.getElementById('mbg-pwd').addEventListener('click',e=>{if(e.target===document.getElementById('mbg-pwd'))window.cerrarReset();});

window.ejecutarReset=async()=>{
  const pwd=document.getElementById('r-pwd').value;
  const pwd2=document.getElementById('r-pwd2').value;
  const uid=document.getElementById('reset-uid').value;
  if(!pwd||pwd.length<6){showErr('La contraseña debe tener al menos 6 caracteres.');return;}
  if(pwd!==pwd2){showErr('Las contraseñas no coinciden.');return;}
  const btn=document.getElementById('btn-reset');btn.disabled=true;btn.innerHTML='⏳ Cambiando...';
  try{
    // Marcar primerLogin=false en Firestore (el reset de pwd real requiere Admin SDK)
    // En ambiente de producción usar Firebase Admin SDK o Cloud Functions
    // Por ahora actualizamos el flag y mostramos instrucción
    await updateDoc(doc(db,'usuarios',uid),{
      primerLogin:false,
      pwdResetPending:true,
      newPwdTemp:pwd,// ⚠️ Solo referencial, reemplazar con Admin SDK en prod
      updatedAt:serverTimestamp()
    });
    showOk('✅ Solicitud de cambio registrada. Para aplicarla en Firebase Auth usa la consola de Firebase o implementa el Admin SDK.');
    cerrarReset();loadUsuarios();
  }catch(e){showErr('Error: '+e.message);}
  btn.disabled=false;btn.innerHTML='🔑 Cambiar contraseña';
};
</script>
</body>
</html>
